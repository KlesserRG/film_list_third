import 'package:flutter/material.dart';

class MainPageDrawer extends StatelessWidget {
  const MainPageDrawer({super.key});

  /*
    Drawer на текущий момент не используется
  */

  @override
  Widget build(BuildContext context) {
    return const Drawer();
  }
}